execute pathogen#infect()

" always show vim-airline status bar
set laststatus=2

" show line numbers
set number

" map Ctrl-n to toggle the nerd tree plugin
map <C-n> :NERDTreeToggle<CR>

" set color scheme and enable syntax highlighting
set t_Co=256
colorscheme grb4
syntax on
